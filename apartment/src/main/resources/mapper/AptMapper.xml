<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.dw.Mapper.AptMapper">
    
    <select id="get">
    
    select * from busan where apartmentname = '썬리치' and dealyearmonth like '2007%'
    
    </select>
    
    <select id="get2">
    
    select apartmentname , count(apartmentname) as count from busan where dealyearmonth like '2007%' group by apartmentname having count(*) > 0
    
    </select>
    
</mapper>